export const LabConfig = {
  title: "Lab 1: Inventory API Server",
  description: `
    <h3>Problem Statement</h3>
    <p>
      Workshop 3 uses a small inventory API. The root CodeForCare
      server only serves static files, so run this workshop with its own
      server file.
    </p>
    <p>
      Open <code>Labs/Workshop3</code> in a terminal and run
      <code>node API.js</code>. Then open <code>http://localhost:3000/</code>.
      Note:  Assuming you are in codeforcare folder(same folder to run node server.js), can type <br>
      cd Labs<br>
      cd Workshop3<br>
      node API.js<br>
    </p>
  `,
  testCases: []
};
